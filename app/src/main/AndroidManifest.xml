<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.clinicaveterinaria">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_isotipo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_isotipo"
        android:supportsRtl="true"
        android:theme="@style/Theme.ClinicaVeterinaria">
        <activity android:name=".DetalleActivity"></activity>
        <activity android:name=".ListaCitaActivity" />
        <activity android:name=".NuevaActivity" />
        <activity
            android:name=".MainActivity"
            android:theme="@style/Theme.ClinicaVeterinaria.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>